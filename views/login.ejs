<!DOCTYPE html>
<html>

<head>
  <title>
    <%=title%>
  </title>
  <%- include ("./partials/header.ejs") %>
    <style>
      label {
        margin-left: 40%;
      }



      .cm-input {
        display: block;
        box-sizing: border-box;
        padding: 10px;
        width: 20%;
        margin: 14px auto;
        border-radius: 20px;
        border: 1px solid #ccc;


      }

      .cm-input:focus {
        outline: 0;
        border-color: #f9cb81;

      }

      .cm-input:invalid {
        border-color: #e41b17;

      }



      .btn-login {
        display: block;
        width: 20%;
        padding: 10px;
        border: 0;
        color: #fff;
        border-radius: 20px;
        cursor: pointer;
        margin-left: 40%;

      }

      .btn-login:focus {
        outline: 0;
      }

      .btn-login:hover {
        opacity: 0.8;
        transition: .3s;
      }

      /* Gradient Background */
      .gr-bg {
        background: rgb(252, 205, 128);
        background: linear-gradient(90deg, rgba(252, 205, 128, 1) 0%, rgba(209, 122, 142, 1) 55%, rgba(220, 159, 174, 1) 100%);
      }

      .codehim-form {
        max-width: 200px;
        min-height: 400px;
        box-sizing: border-box;
        background: rgba(255, 255, 255, 0.6);
        box-shadow: 4px 2px 16px rgba(0, 0, 0, 0.4);
        border-radius: 8px;
        margin: 20px auto 0 auto;
        padding: 25px;
        color: #414141;

      }
    </style>
</head>

<body>

  <%if(errors.length>0){%>
    <%errors.forEach((item)=>{%>
      <div class="alert alert-danger" role="alert">

        <%=item.msg%>
      </div>
      <% });%>
        <% } %>


          <form action="/login" method="post" id="myform">
            <br><br>


            <label for="email"><i class="fa fa-envelope"></i> Email:</label>
            <input type="email" id="email" class="cm-input" placeholder="Enter your email address" name="email">
            <label for="pass"><i class="fa fa-lock"></i> Password:</label>
            <input id="pass" type="password" name="password" class="cm-input" placeholder="Enter your password">
            <button type="submit" class="btn-login  gr-bg">Login</button>



          </form>

</body>

</html>